a1 = 1;
f1 = fun x1 -> (
  f2 = fun x2 -> (
    target = fun x3 -> (
      rt = 1
    )
  )
);

apply = fun g -> (
  three = 3;

  x = input;
  one = 1;
  cx = x == one;
  h = g three;
  rf = cx ? (
    h = g three;
  ) : (
    du = 7
  );
);

r1 = apply f1;
r2 = apply r1;
r3 = apply r2;
