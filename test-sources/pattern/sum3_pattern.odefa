
x = input;
f = fun s -> (
    f0 = fun a -> (
        zero_a = 0;
        b = a == zero_a;
        r = b ? (
                r1 = 0
            ) : (
                cs = s ~ fun;
                ss = cs ? (ts = s s) : (fs = abort);

                one_b = 1;
                v = a - one_b;

                css = ss ~ fun;
                v2 = css ? (tss = ss v) : (fss = abort);

                r2 = a + v2
            );
    );
);
ff = f f;
x3 = 3;

cff = ff ~ fun;
z = cff ? (tz = ff x3) : (fz = abort);

c = x == z;
e = c ? (
    target = 42
) : (
    void = 0
)
