
x = input;
sum = fun s -> (
    f0 = fun a -> (
        zero = 0;
        b = a == zero;
        r = b ? (
                r1 = 0
            ) : (
                ss = ss
                one_b = 1;
                v = a - one_b;
                v2 = ss v
                r2 = a + v2
            );
    );
);
ff = sum sum;
x3 = 3;

cff = ff ~ fun;
z = cff ? (tz = ff x3) : (fz = abort);

c = x == z;
e = c ? (
    target = 42
) : (
    void = 0
)
