# EXPECT-WELL-FORMED
# EXPECT-INPUT-SEQUENCES-REACH target [ -1, 0 ]
## It takes 7318 steps to compute. The number is mainly controlled by two loop count (line 8 and line 20 )mak

let loop_end = 0
in let f m n =
  let rec f_loop n sum i =
    if i == 2
    then sum
    else (
        let a = n % 2
        in let sum_next = 
                sum + (if a == 0 then 0 else (a + 1))
        in let n_next = n / 2
        in f_loop n_next sum_next (i+1)
    )
  in f_loop n 0 0
in let g m n =
  let rec g_loop i =
    if i == 2
    then loop_end
    else (
      let ignored =
        if m == i
        then f m n
        else 0
      in g_loop (i+1)
    )
  in g_loop 0
in let m = input
in let n = input
in let r = g m n
in if r == 0
    then let target = 1 in target
    else false