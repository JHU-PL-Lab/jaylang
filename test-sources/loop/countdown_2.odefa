zero = 0;
one = 1;
two = 2;
f = fun s -> (
  r1 = fun x -> (
    num = input;
    fc = num == zero;
    r2 = fc ?
      ( t = 0; ) :
      ( ss = s s;
        r3 = ss x;
        r4 = r3 + one;
      );
  );
);
count = f f;
total = count zero;
c = total == two;
final = c ? ( target = 0 ) : ( bad = 0 );