# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR-COUNT 1
# EXPECT-TYPE-ERROR ~result [] "s = r z" "r" "{ zero = z, one = o }" "fun" "{zero, one}"

z = 0;
o = 1;
r = { zero = z, one = o };
s = r z;
t = s + s;
