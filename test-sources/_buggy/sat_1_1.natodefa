# EXPECT-WELL-FORMED
# EXPECT-INPUT-SEQUENCES-REACH target [ 0 ]
# EXPECT-REQUIRED-INPUT-SEQUENCE-GENERATION-STEPS 30000

let x = input in

let phi formula = formula.x1
in
let try f =
  (f true) or (f false)
in
let sat_solve p =
  try (fun n1 ->
        let arg = {x1 = n1} in
        p arg
      )
in
let result = sat_solve phi in
if result then
  let target = 1 in 1
else
  0