# []
# [-(r,$tt)]
f = fun x -> (
  # [+(t1,f)]
  # [-(r,$tt);+(t1,f)]
  g = fun y -> (
  
    #-1 [+(a,g)]
    #-1 [-(r,$tt);+(a,g)]

    #-2 [+(b,g)]
    #-2 [-(r,$tt);+(b,g)]
    t = input
  )
);
z = 0;
t1 = f z;
a = t1 z;
b = t1 z;
c = a < b;

# []
# [-(r,$tt)]
r = c ? (

  # [+(r,$tt)]
  # []
  target = 1
) : (
  void = 42
)