
(* let solve_for_target (target : Branch.t) (session : Session.t) =
  let formulas =
    Formula_tracker.all_formulas
      session.formula_tracker
      ~target
      ~aborts:[]
      ~max_steps:[] *)