(include_subdirs unqualified)

(library
 (name new_concolic)
 (public_name jay.new_concolic)
 (libraries
  core
  fmt
  lwt
  lwt.unix
  z3
  logs
  logs.lwt
  logs.fmt
  monadlib
  lang
  translate
  psq) ; psq added for queue of target branches
 (synopsis "Concolic evaluator on embedded-type programs")
 (inline_tests)
 (preprocess
  (pps
   ppx_sexp_conv
   ppx_compare
   ocaml-monadic
   ppx_deriving.show
   lwt_ppx
   ;bisect_ppx ; should comment when not wanting report because it adds overhead
   ; for profiling
   landmarks-ppx
   --auto)))