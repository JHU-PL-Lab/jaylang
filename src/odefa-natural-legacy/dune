(copy_files# parser/*.{ml,mli})

(copy_files parser/*.{mll,mly})

(ocamllex on_lexer)

(menhir
 (flags --explain)
 (modules on_parser))

(library
 (name odefa_natural_old)
 (public_name odefa.natural_old)
 (synopsis "Translator for Odefa Natural")
 (libraries batteries jhupllib monadlib odefa.ast)
 (preprocess
  (pps ppx_deriving.std ppx_deriving_yojson ocaml-monadic))
 (flags
  (:standard -w -39) ;for ppx_deriving_yojson, which generates extra rec flags
  ))
