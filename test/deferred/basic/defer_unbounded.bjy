(***
  (
    (speed Fast)
    (typing Exhausted)
  )
*)

(* This expression never finishes ... *)
let _ = defer (
  (fun x -> x x) (defer (fun x -> x x))
)

(* ... so this error cannot happen *)
let x = assert false
