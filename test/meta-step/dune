(alias
  (name megatest)
  ; (deps test.exe (glob_files ../../test-sources/*))
  (action (run ./test.exe))
)

(alias
  (name test_dbmc)
  (action (run ./test_dbmc.exe))
)

(executables
  (names test test_dbmc)
  (libraries
    batteries
    jhupllib
    odefa.ast
    odefa.ddpa
    odefa.parser
    odefa.symbolic-interpreter
    odefa.test-generation
    odefa.toploop
    odefa.natural
    dmbc_solver
  )
  (preprocess
    (pps
      ppx_deriving.std
    )
  )
)