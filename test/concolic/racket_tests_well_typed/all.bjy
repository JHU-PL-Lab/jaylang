# error fixed by making p return bool

let rec all (p : 'a -> bool) (xs : List 'a) : bool =
  match xs with
  | [] -> true
  | x :: xss -> 
    match xss with
    | [] -> p x 
    | _ -> (p x) and (all p xss)
    end
  end
in

all