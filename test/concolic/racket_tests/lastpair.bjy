# predicate violated because can return empty list

let is_nonempty ls = 
  match ls with
  | [] -> false
  | _ -> true
  end
in

let rec lastpair (x : {. List 'a | is_nonempty }) : {. List 'a | is_nonempty } =
  match x with
  | [] -> []
  | h :: xs -> lastpair xs 
  end
in

lastpair