
let t = (int -> int) && (bool -> bool)
in

let (f : t) = 
  let g (x : 'a) : 'a =
    match x with
    | int -> x + 1 # ERROR: type casing on polymorphic type
    | bool -> not x
    | _ -> x
    end
  in
  g
in

f
