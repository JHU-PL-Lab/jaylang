(***
  (
    (features (Mu_types Variants Records))
    (speed Fast)
    (typing Exhausted)
    (flags "-s")
  )
*)

let x : mu t. `R of { a : bool ; b : t } | `B of bool =
  `R { a = false ; b = `B true }
