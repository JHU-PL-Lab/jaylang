(*** (
  (features (Variants Mu_types Records))
) *)

let tree_type = Mu tt.
  | `Node of { left : tt; right : tt }
  | `Leaf of { leaf : int }

let x : tree_type = 
  `Node { left = `Leaf { leaf = 0 } ; right = `Leaf { leaf = 0 } }
