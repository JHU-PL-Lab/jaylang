(***
  (
    (features (Records Higher_order_functions Subtyping))
    (speed Fast)
    (typing well_typed)
  )
*)

let fun_sig = { a : int ; b : int } -> int

let f (x : { a : int }) : int = x.a

let _ : fun_sig = f
