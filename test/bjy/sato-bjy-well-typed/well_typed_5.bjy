(***
  (
    (features (Higher_order_functions))
    (speed Fast)
    (typing well_typed)
  )
*)

let twice (f : int -> int) (x : int) : int = f (f x)

let inc (x : int) : int = x + 1

let res = twice inc 2
