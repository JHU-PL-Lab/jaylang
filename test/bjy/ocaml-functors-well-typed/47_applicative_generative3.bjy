(***
  (
    (speed Fast)
    (typing Exhausted)
    (flags "")
  )
*)

(*
module F(X:empty) = struct end
module R =
  F(struct end)();;
*)

let empty = sig end

let F(X:empty) : sig end = struct end

let R = F(struct end)
