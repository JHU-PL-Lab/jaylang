(***
  (
    (speed Fast)
    (typing Well_typed)
    (flags "")
  )
*)

(*
module type a = sig type a end

module type empty = sig end

module F(X:a): sig module F:empty -> empty end = struct module F = X end
*)


let a = sig val a : type end

let empty = sig end

let F (X : a) : sig val F : empty -> a end = struct let F _ = X end
