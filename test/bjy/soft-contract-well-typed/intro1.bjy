(***
  (
    (features (Refinement_types Higher_order_functions))
    (speed Fast)
    (typing Well_typed)
  )
*)

let f x g =
  g (x + 1)

let h (y : { c : int | c > 1 }) : unit =
  {}

let main (n : int) : unit =
  if n > 0
  then f n h
  else {}
