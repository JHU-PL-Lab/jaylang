(***
  (
    (features (Polymorphic_types Recursive_functions))
    (speed Fast)
    (typing Well_typed)
    (flags "-s")
  )
*)

let rec length (type a) (ls : list a) : int =
  match ls with
  | [] -> 0
  | _ :: tl -> 1 + length a tl
  end
