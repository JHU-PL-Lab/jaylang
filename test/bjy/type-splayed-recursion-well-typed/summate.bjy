(***
  (
    (features (Refinement_types Recursive_functions))
    (speed Fast)
    (typing Exhausted)
    (flags "-s")
  )
*)

let rec summate (n : int | n >= 0) : int =
  if n == 0 
  then 0 
  else n + summate (n - 1)
