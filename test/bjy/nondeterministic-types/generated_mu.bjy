(***
  (
    (features (Mu_types Variants Other))
    (reasons (Mu_types Other))
    (speed Fast)
    (typing Ill_typed)
  )
*)

let f (g : int -> bool) : unit =
  let t = mu t. if g 0 then `A of int else `B of t in (* ERROR: use of input in a type *)
  let h : t -> t = fun x -> x in (* ERROR: this fails. We prefer the error is at the site of the mu type, but at least this fails _somewhere_. *)
  ()
