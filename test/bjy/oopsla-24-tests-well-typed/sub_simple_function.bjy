(*** (
  (features (Higher_order_functions Subtyping Records Return_type))
) *)
(* subtyping of functions *)

let t = ({ a : int ; b : int } -> int) -> int

let f (h : { a : int ; b : int } -> int) : int =
  0

let g : t =
  f