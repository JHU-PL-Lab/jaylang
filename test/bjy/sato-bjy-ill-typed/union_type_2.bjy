(***
  (
    (features (Variants Records))
    (reasons (Variants))
    (speed Fast)
    (typing Ill_typed)
  )
*)

let x : { a : bool ; b : `R of { a : bool ; b: int } | `I of int } =
  { a = true; b = true }
