let rec testFun l = 
  match l with
  | [] -> true
  | hd :: tl -> if (hd > 10) then testFun tl else false
  end

let userinput = input

let val = userinput

let (x : { List int | testFun } ) =
  [ val ; val ]
