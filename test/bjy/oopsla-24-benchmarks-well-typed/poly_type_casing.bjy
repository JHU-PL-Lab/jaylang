
let (t : type) = ((``Int int) -> int) && ((``Bool bool) -> bool)

let (t : type) = (v : ``Int int || ``Bool bool) ->
  match v with
  | `Int i -> int
  | `Bool b -> bool
  end

let (f : t) = 
  fun x ->
    match x with
    | `Int i -> i + 1
    | `Bool b -> not b
    end