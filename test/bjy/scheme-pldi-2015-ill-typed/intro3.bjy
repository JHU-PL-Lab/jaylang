(***
  (
    (features (Refinement_types Dependent_arrows Higher_order_functions Usage_error))
    (reasons (Refinement_types Dependent_arrows Usage_error))
    (speed Fast)
    (typing Ill_typed)
  )
*)

let f x g =
  g (x + 1)

let h (z <- int) (y : { y : int | z > y }) : unit = (* ERROR: should be `z < y` instead of other way around *)
  {}

let main (n : int) : unit =
  if n > 0
  then f n (h n)
  else {}
