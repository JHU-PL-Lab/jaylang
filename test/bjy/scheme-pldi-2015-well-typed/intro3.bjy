# error is that > in `h` should be <

let f x g =
  g (x + 1)

letd h (z : int) : { int | fun y -> z < y } -> unit =
  fun y -> 
    {}

let main (n : int) : unit =
  if n > 0
  then f n (h n)
  else {}
