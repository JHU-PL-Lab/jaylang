(***
  (
    (features (Refinement_types Dependent_arrows Higher_order_functions))
    (speed Fast)
    (typing Well_typed)
  )
*)

let f x g =
  g (x + 1)

let h (z <- int) (y : { y : int | z < y }) : unit =
  {}

let main (n : int) : unit =
  if n > 0
  then f n (h n)
  else {}
