(***
  (
    (speed Fast)
    (typing Ill_typed)
    (flags "")
  )
*)
(*
module type a

module type empty = sig end

module F(X:a -> a): empty = X
*)

let a = abstract

let empty = sig end

(* ERROR: X is a functor, not the empty module *)
let F (X : a -> a) : empty = X

(* We argue there's no sensible well-typed version of this test that keeps the meaning *)
