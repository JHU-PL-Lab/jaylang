(***
  (
    (speed Fast)
    (typing Ill_typed)
    (flags "")
  )
*)

(*
module type x = sig type x end

module type empty = sig end
module Empty = struct end

module F_empty(_:empty) = Empty
module M: x = F_empty
*)

let x = sig val x : type end

let empty = sig end
let Empty = struct end

let F_empty (_ : empty) : empty = Empty
let M : x = F_empty (* ERROR: a functor is not a structure *)
