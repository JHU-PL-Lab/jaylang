(***
  (
    (speed Fast)
    (typing Ill_typed)
    (flags "")
  )
*)

(*
module F(X:sig type t end) = struct end
module M = F(struct type x end)
*)

let F (X : sig val t : type end) : sig end = struct end

(* ERROR: the type should be called t, not x*)
let M = F (struct let x = abstract end)
