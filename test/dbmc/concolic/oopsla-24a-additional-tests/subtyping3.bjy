# subtype of union type

let f (i : int) (b : bool) (a : 'a) : (int || bool || List 'a) =
  i :: [] # Error is that `i` is int and is not necessarily 'a, so type of returned list is not always subtype
in

f