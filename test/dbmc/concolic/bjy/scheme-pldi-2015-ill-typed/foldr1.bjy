# error is that xs should be nonempty

let rec foldr1 (f : 'a -> 'a -> 'a) (xs : List 'a) : 'a =
  match xs with
  | [] -> assert false
  | z :: zs ->
    match zs with
    | [] -> z
    | _ -> f z (foldr1 f zs)
    end 
  end
in

foldr1