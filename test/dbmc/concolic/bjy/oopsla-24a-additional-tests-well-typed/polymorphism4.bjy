# Polymorphic compose

# TODO: make ill-typed for real

let compose (b_c : 'b -> 'c) (a_b : 'a -> 'b) (a : 'a) : 'c =
  b_c (a_b a)
in

let r =
  compose
    (fun x -> x)
    (fun y -> y)
    4 # ERROR originates from wrap with this argument, but I'm unsure why. TODO: figure out why
in

r
