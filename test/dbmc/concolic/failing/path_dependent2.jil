
x = input;
x_copy = x;

target_input = 1;
x_is_target_cond = x == target_input;

x_is_target_branch = x_is_target_cond ? (
  first_branch_true_return = 0
) : (
  first_branch_false_return = target_input
);

second_cond = x_is_target_branch == x_copy;

second_branch = second_cond ? (
  clause2 = 2
) : (
  clause3 = 3
)