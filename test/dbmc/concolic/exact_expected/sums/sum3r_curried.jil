# This program just has a tricky recursive sum to solve. All branches can be hit.
# This checks that the solver can handle recursive functions with several inputs.

other_zero = 0;
x = input;
f = fun s -> (
    f0 = fun ra -> (
        fc = fun rc -> (
            a = ra.val;
            other_one_a = 0;
            b = a == other_one_a;
            r = b ? (
                    r1 = 0
                ) : (
                    tc = other_zero < x;
                    w1 = tc ? (
                        ss = s s;
                        other_one_b = 1;
                        v = a - other_one_b;
                        rv = {val = v};
                        sv2 = ss rv;
                        v2 = sv2 other_zero;
                        r2 = a + v2
                    ) : (
                        wff = 2;
                    )
                );
        );
    );
);
ff = f f;
x3 = 3;
rx3 = { val = x3 };
zz = ff rx3;
z = zz other_zero;
c = x == z;
e = c ? (
    target = 42
) : (
    void = 0
)
