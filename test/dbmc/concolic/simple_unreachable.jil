# This program has a condition that does not depend on x, so the solver cannot satisfy the other side.

# EXPECTED BRANCH INFORMATION
# always_true_branch: True=Hit; False=Unsatisfiable
# unreachable_branch: True=Unreachable; False=Unreachable


x = input;

one = 1;
cx = one < x;

always_true_cond = one == one;

always_true_branch = always_true_cond ?
  (
    y = cx ?
      ( ret_true_y = 111 ) :
      ( ret_false_y = 222 )
  )
  :
  (
    unreachable_cond = x < one;
    unreachable_branch = unreachable_cond ?
      ( u = 1 ) :
      ( v = 0 )
  )