# This program checks that we can use two inputs, but it's not sophisticated. It's just really simple.

# EXPECTED BRANCH INFORMATION 
# x_eq_y_branch: True=Hit; False=Hit
# always_false_branch: True=Unsatisfiable; False=Hit

x = input;
y = input;

x_eq_y_cond = x == y;

x_eq_y_branch = x_eq_y_cond ?
  (
    always_false_cond = x < y;

    always_false_branch = always_false_cond ?
      (
        unreachable_clause = abort
      ) :
      (
        reachable_clause = 100
      )
  ) :
  (
    clause0 = 0
  )