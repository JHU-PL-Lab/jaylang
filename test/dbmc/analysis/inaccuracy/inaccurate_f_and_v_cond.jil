a1 = 1;
f1 = fun x1 -> (
  f2 = fun x2 -> (
    x = input;
    one = 1;
    cx = x == one;
    rf = cx ? (
      target = one;
    ) : (
      du = 7
    )
  )
);

apply = fun g -> (
  three = 3;
  h = g three;
);

r1 = apply f1;
r2 = apply r1
