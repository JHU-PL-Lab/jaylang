# EXPECT-WELL-FORMED
# EXPECT-INPUT-SEQUENCES-REACH target [0]

let dummy = 1 in

let rec count d = 
  let x = input in
  if x == 0 then
    0
  else
    1 + (count d)
  in

let ca = count 0 in
let cb = count 0 in

if ca == 2 * cb and 2 < cb then
  let target = 1 in 1
else
  0
