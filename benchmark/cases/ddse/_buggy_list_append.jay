# Std.List - start

let hd l = 
  match l with
  | x :: xs -> x
  | [] -> (0 0)
  end
in
let tl l = 
  match l with
  | x :: xs -> xs
  | [] -> (0 0)
  end
in

# Std.List - end

let x = input in
let x2 = 2 in
let x3 = 3 in
let x4 = 4 in
let b = tl (tl (tl (tl [1, 2, 3, 4, 5, 6]))) in

if (hd b == x) then
  let target = 1 in 1
else
  0