let x = input in
let y = input in
let id x = x in 
let rec f n =
  if n <= 1 then
    1
  else 
    n * (f (n-1))
  in
let rec g n =
  if n <= 1 then
    1
  else
    n * (g (n-1))
  in 
let sum = 
  # our testcase
  #((id f) 3) + ((id g) x)
  # original testcase
  ((id f) x) + ((id g) y) 
  in
if (sum == 3628806) then
#if (sum == 30) then
  let target = 1 in 1
else 
  0