#lang racket

(provide (all-defined-out))

(struct Experiment (name tests) #:transparent)
(struct Test (name test-cases) #:transparent)
(struct Case (source subject result) #:transparent)
(struct Subject (analysis k) #:transparent)
(struct Result (running-time) #:transparent)

(define experiments
  `(,(Experiment
      'baseline
      `(,(Test 'church `(,(Case 'church (Subject 'ddpa 0) #f) ,(Case 'church (Subject 'little-store 0) #f)))
        ,(Test 'cpstak `(,(Case 'cpstak (Subject 'ddpa 0) #f) ,(Case 'cpstak (Subject 'little-store 0) #f)))
        ,(Test 'deriv `(,(Case 'deriv (Subject 'ddpa 0) #f) ,(Case 'deriv (Subject 'little-store 0) #f)))
        ,(Test 'primtest `(,(Case 'primtest (Subject 'ddpa 0) #f) ,(Case 'primtest (Subject 'little-store 0) #f)))
        ,(Test 'regex `(,(Case 'regex (Subject 'ddpa 0) #f) ,(Case 'regex (Subject 'little-store 0) #f)))
        ,(Test 'rsa `(,(Case 'rsa (Subject 'ddpa 0) #f) ,(Case 'rsa (Subject 'little-store 0) #f)))
        ,(Test 'sat-2 `(,(Case 'sat-2 (Subject 'ddpa 0) #f) ,(Case 'sat-2 (Subject 'little-store 0) #f)))))
    ,(Experiment
      'k=2
      `(,(Test 'church `(,(Case 'church (Subject 'ddpa 2) #f) ,(Case 'church (Subject 'little-store 2) #f)))
        ,(Test 'cpstak `(,(Case 'cpstak (Subject 'ddpa 2) #f) ,(Case 'cpstak (Subject 'little-store 2) #f)))
        ,(Test 'deriv `(,(Case 'deriv (Subject 'ddpa 2) #f) ,(Case 'deriv (Subject 'little-store 2) #f)))
        ,(Test 'primtest `(,(Case 'primtest (Subject 'ddpa 2) #f) ,(Case 'primtest (Subject 'little-store 2) #f)))
        ,(Test 'regex `(,(Case 'regex (Subject 'ddpa 2) #f) ,(Case 'regex (Subject 'little-store 2) #f)))
        ,(Test 'rsa `(,(Case 'rsa (Subject 'ddpa 2) #f) ,(Case 'rsa (Subject 'little-store 2) #f)))
        ,(Test 'sat-2 `(,(Case 'sat-2 (Subject 'ddpa 2) #f) ,(Case 'sat-2 (Subject 'little-store 2) #f)))))
    ,(Experiment
      'k=4
      `(,(Test 'church `(,(Case 'church (Subject 'ddpa 4) #f) ,(Case 'church (Subject 'little-store 4) #f)))
        ,(Test 'cpstak `(,(Case 'cpstak (Subject 'ddpa 4) #f) ,(Case 'cpstak (Subject 'little-store 4) #f)))
        ,(Test 'deriv `(,(Case 'deriv (Subject 'ddpa 4) #f) ,(Case 'deriv (Subject 'little-store 4) #f)))
        ,(Test 'primtest `(,(Case 'primtest (Subject 'ddpa 4) #f) ,(Case 'primtest (Subject 'little-store 4) #f)))
        ,(Test 'regex `(,(Case 'regex (Subject 'ddpa 4) #f) ,(Case 'regex (Subject 'little-store 4) #f)))
        ,(Test 'rsa `(,(Case 'rsa (Subject 'ddpa 4) #f) ,(Case 'rsa (Subject 'little-store 4) #f)))
        ,(Test 'sat-2 `(,(Case 'sat-2 (Subject 'ddpa 4) #f) ,(Case 'sat-2 (Subject 'little-store 4) #f)))))
    #;,(Experiment
        'baseline-full
        `(,(Test 'ack `(,(Case 'ack (Subject 'ddpa 0) #f) ,(Case 'ack (Subject 'little-store 0) #f)))
          ,(Test 'blur `(,(Case 'blur (Subject 'ddpa 0) #f) ,(Case 'blur (Subject 'little-store 0) #f)))
          ,(Test 'church `(,(Case 'church (Subject 'ddpa 0) #f) ,(Case 'church (Subject 'little-store 0) #f)))
          ,(Test 'cpstak `(,(Case 'cpstak (Subject 'ddpa 0) #f) ,(Case 'cpstak (Subject 'little-store 0) #f)))
          ,(Test 'deriv `(,(Case 'deriv (Subject 'ddpa 0) #f) ,(Case 'deriv (Subject 'little-store 0) #f)))
          ,(Test 'eta `(,(Case 'eta (Subject 'ddpa 0) #f) ,(Case 'eta (Subject 'little-store 0) #f)))
          ,(Test 'facehugger `(,(Case 'facehugger (Subject 'ddpa 0) #f) ,(Case 'facehugger (Subject 'little-store 0) #f)))
          ,(Test 'flatten `(,(Case 'flatten (Subject 'ddpa 0) #f) ,(Case 'flatten (Subject 'little-store 0) #f)))
          ,(Test 'kcfa-2 `(,(Case 'kcfa-2 (Subject 'ddpa 0) #f) ,(Case 'kcfa-2 (Subject 'little-store 0) #f)))
          ,(Test 'kcfa-3 `(,(Case 'kcfa-3 (Subject 'ddpa 0) #f) ,(Case 'kcfa-3 (Subject 'little-store 0) #f)))
          ,(Test 'loop2 `(,(Case 'loop2-1 (Subject 'ddpa 0) #f) ,(Case 'loop2-1 (Subject 'little-store 0) #f)))
          ,(Test 'map `(,(Case 'map (Subject 'ddpa 0) #f) ,(Case 'map (Subject 'little-store 0) #f)))
          ,(Test 'mj09 `(,(Case 'mj09 (Subject 'ddpa 0) #f) ,(Case 'mj09 (Subject 'little-store 0) #f)))
          ,(Test 'primtest `(,(Case 'primtest (Subject 'ddpa 0) #f) ,(Case 'primtest (Subject 'little-store 0) #f)))
          ,(Test 'regex `(,(Case 'regex (Subject 'ddpa 0) #f) ,(Case 'regex (Subject 'little-store 0) #f)))
          ,(Test 'rsa `(,(Case 'rsa (Subject 'ddpa 0) #f) ,(Case 'rsa (Subject 'little-store 0) #f)))
          ,(Test 'sat-1 `(,(Case 'sat-1 (Subject 'ddpa 0) #f) ,(Case 'sat-1 (Subject 'little-store 0) #f)))
          ,(Test 'sat-2 `(,(Case 'sat-2 (Subject 'ddpa 0) #f) ,(Case 'sat-2 (Subject 'little-store 0) #f)))
          ,(Test 'sat-3 `(,(Case 'sat-3 (Subject 'ddpa 0) #f) ,(Case 'sat-3 (Subject 'little-store 0) #f)))
          ,(Test 'state `(,(Case 'state (Subject 'ddpa 0) #f) ,(Case 'state (Subject 'little-store 0) #f)))
          ,(Test 'tak `(,(Case 'tak (Subject 'ddpa 0) #f) ,(Case 'tak (Subject 'little-store 0) #f)))))
    #;,(Experiment
        'k=4-full
        `(,(Test 'ack `(,(Case 'ack (Subject 'ddpa 4) #f) ,(Case 'ack (Subject 'little-store 4) #f)))
          ,(Test 'blur `(,(Case 'blur (Subject 'ddpa 4) #f) ,(Case 'blur (Subject 'little-store 4) #f)))
          ,(Test 'church `(,(Case 'church (Subject 'ddpa 4) #f) ,(Case 'church (Subject 'little-store 4) #f)))
          ,(Test 'cpstak `(,(Case 'cpstak (Subject 'ddpa 4) #f) ,(Case 'cpstak (Subject 'little-store 4) #f)))
          ,(Test 'deriv `(,(Case 'deriv (Subject 'ddpa 4) #f) ,(Case 'deriv (Subject 'little-store 4) #f)))
          ,(Test 'eta `(,(Case 'eta (Subject 'ddpa 4) #f) ,(Case 'eta (Subject 'little-store 4) #f)))
          ,(Test 'facehugger `(,(Case 'facehugger (Subject 'ddpa 4) #f) ,(Case 'facehugger (Subject 'little-store 4) #f)))
          ,(Test 'flatten `(,(Case 'flatten (Subject 'ddpa 4) #f) ,(Case 'flatten (Subject 'little-store 4) #f)))
          ,(Test 'kcfa-2 `(,(Case 'kcfa-2 (Subject 'ddpa 4) #f) ,(Case 'kcfa-2 (Subject 'little-store 4) #f)))
          ,(Test 'kcfa-3 `(,(Case 'kcfa-3 (Subject 'ddpa 4) #f) ,(Case 'kcfa-3 (Subject 'little-store 4) #f)))
          ,(Test 'loop2 `(,(Case 'loop2-1 (Subject 'ddpa 4) #f) ,(Case 'loop2-1 (Subject 'little-store 4) #f)))
          ,(Test 'map `(,(Case 'map (Subject 'ddpa 4) #f) ,(Case 'map (Subject 'little-store 4) #f)))
          ,(Test 'mj09 `(,(Case 'mj09 (Subject 'ddpa 4) #f) ,(Case 'mj09 (Subject 'little-store 4) #f)))
          ,(Test 'primtest `(,(Case 'primtest (Subject 'ddpa 4) #f) ,(Case 'primtest (Subject 'little-store 4) #f)))
          ,(Test 'regex `(,(Case 'regex (Subject 'ddpa 4) #f) ,(Case 'regex (Subject 'little-store 4) #f)))
          ,(Test 'rsa `(,(Case 'rsa (Subject 'ddpa 4) #f) ,(Case 'rsa (Subject 'little-store 4) #f)))
          ,(Test 'sat-1 `(,(Case 'sat-1 (Subject 'ddpa 4) #f) ,(Case 'sat-1 (Subject 'little-store 4) #f)))
          ,(Test 'sat-2 `(,(Case 'sat-2 (Subject 'ddpa 4) #f) ,(Case 'sat-2 (Subject 'little-store 4) #f)))
          ,(Test 'sat-3 `(,(Case 'sat-3 (Subject 'ddpa 4) #f) ,(Case 'sat-3 (Subject 'little-store 4) #f)))
          ,(Test 'state `(,(Case 'state (Subject 'ddpa 4) #f) ,(Case 'state (Subject 'little-store 4) #f)))
          ,(Test 'tak `(,(Case 'tak (Subject 'ddpa 4) #f) ,(Case 'tak (Subject 'little-store 4) #f)))))))
