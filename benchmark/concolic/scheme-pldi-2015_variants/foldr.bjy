# should be f xx (foldr f z xxs) and xs should be int list

let rec foldr (f : int -> bool -> bool) (z : bool) (xs : List bool) : bool =
  match xs with
  | [] -> z
  | xx :: xxs -> f (foldr f z xxs) xx
  end
in
foldr