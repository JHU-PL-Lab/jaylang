# error is n should be greater than 1

let rec mult x y =
  if x <= 0 or y <= 0
  then 0
  else x + mult x (y - 1)
in

let h y =
  let b = (y + y <= mult y y)
  in
  b
in

let main (n : int) : {. bool | fun b -> b } =
  h n
in

main