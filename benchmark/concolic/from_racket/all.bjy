# simple error with return type in singleton case

let rec all (p : 'a -> 'a) (xs : List 'a) : bool =
  match xs with
  | [] -> true
  | x :: xss -> 
    match xss with
    | [] -> p x # error is this needs to be a boolean
    | _ -> (p x) and (all p xss)
    end
  end
in

all