# it's expected that the evaluator quits before finishing `g 10000`

let is_10000 x = x == 10000
in

let f (x : {. int | is_10000}) : bool = 
  let rec g (x : int) : int =
    if x == 0
    then 0
    else g (x - 1)
  in
  g x # ERROR: returns int here
in

f